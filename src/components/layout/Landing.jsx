const Landing = () => {
  return (
    <div
      style={{
        height: "90vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        flexDirection: "column",
      }}
    >
      <h1>Find Your Dream Job</h1>

      <p>Apply for jobs easily with our portal.</p>
    </div>
  );
};

export default Landing;
