const initialState = {
  jobs: [],
  job: null,
  loading: false,
};

const jobReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default jobReducer;
